
<%= @cocktail.name %>
<ul class="list-unstyled">
  <% @cocktail.doses.each do |dose| %>
    <li><%= dose.description %></li>
    <li><%= dose.ingredient.name %></li>
    <%= link_to cocktail_dose_path(@cocktail, dose),
            method: :delete,
            data: { confirm: "Are you sure you want to delete this?" } do  %>
            <%= "Delete this dose" %>
    <% end %>
  <% end %>
</ul>

    <%= cl_image_tag @cocktail.photo, :width=>300, :height=>300, :gravity=>"face", :radius=>"max", :crop=>"fill",
    :background=>"#F4F4F4" %>

<br>
<div class="container">
  <h2 class="text-center">Add a dose to this cocktail</h2>
  <div class="row">
    <%= simple_form_for [@cocktail, @dose] do |f| %>
      <!--Needs [] when you have 2 arguements-->
      <div class="col-md-6 col-md-offset-3">
        <%= f.input :description %>
        <%= f.association :ingredient %>
        <!-- association allows for a drop down list-->
        <%= f.submit %>
      </div>
    <% end %>
    <br>
    <div class="col-md-6 col-md-offset-3">
      <%= link_to "Back to list", cocktails_path, class: "btn btn-primary" %>
    </div>
  </div>
</div>



